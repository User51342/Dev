<UserControl
    x:Class="WlanDetection.Views.ScannerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WlanDetection.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="36" Grid.RowSpan="2" >WiFi Scanner</TextBlock>
        <StackPanel Name="spButtons" Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
            <Button Name="btnScan" Command="{Binding ScanStartCommand}" Grid.Row="1" Content="{Binding StartScanButtonText}"></Button>
            <TextBlock Padding="10" Text="{Binding ActualCoordinates}"></TextBlock>
        </StackPanel>
        <TextBox Name="txbReport" TextWrapping="Wrap" AcceptsReturn="True"
    Grid.Row="2"></TextBox>
    </Grid>
</UserControl>
